services:
  # Plex - Media Server
  plex:
    image: plexinc/pms-docker:latest
    container_name: plex
    # host networking so Plex sees real client IPs (otherwise Docker bridge NAT
    # rewrites every remote source to 172.18.0.1 and Plex classifies all remote
    # streams as LAN, defeating bandwidth limits and "Remote Quality" presets)
    network_mode: host
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    # profiles: ["media", "all"]
    devices:
      - /dev/dri:/dev/dri # for hardware transcoding
    volumes:
      - $DOCKERDIR/appdata/plex:/config
      - $DATADIR/library:/data/media # Media Folder 1
      - /dev/shm:/data/transcode # Offload transcoding to RAM if you have enough RAM
    environment:
      TZ: $TZ
      HOSTNAME: "myPlex"
      PLEX_UID: $PUID
      PLEX_GID: $PGID
      ADVERTISE_IP: "http://$SERVER_IP:32400/"
      ALLOWED_NETWORKS: $LOCAL_IPS
    labels:
      - "traefik.enable=true"
      # HTTP Routers
      - "traefik.http.routers.plex-rtr.entrypoints=websecure"
      - "traefik.http.routers.plex-rtr.rule=Host(`plex.$DOMAINNAME_1`)"
      # Middlewares
      # - "traefik.http.routers.plex-rtr.middlewares=chain-oauth@file"
      # - "traefik.http.routers.plex-rtr.middlewares=chain-basic-auth@file"
      # HTTP Services
      - "traefik.http.routers.plex-rtr.service=plex-svc"
      - "traefik.http.services.plex-svc.loadbalancer.server.port=32400"
      - "com.centurylinklabs.watchtower.enable=true"
